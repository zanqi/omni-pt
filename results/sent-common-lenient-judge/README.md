The 2026-08-24 sent-common runs, kept because the re-run overwrites their paths.

These were scored under the *lenient* answer rubric: the pre-375dc9b
ANSWER_JUDGE_SYSTEM (full credit for acting on the command without naming its
specifics back) and, in the typejudge pass, --no-restate-judge. The track now
enforces restatement in both passes, so these numbers are NOT comparable to the
current results/bab_*_sent-common*.jsonl and must not share a table with them.
